using Xunit;
using Microsoft.AspNetCore.Mvc.Testing;
using Fepa.API;

namespace Fepa.API.Tests
{
    /*
    public class AuthIntegrationTests : IClassFixture<WebApplicationFactory<Program>>
    {
        private readonly WebApplicationFactory<Program> _factory;
        private readonly HttpClient _client;

        public AuthIntegrationTests(WebApplicationFactory<Program> factory)
        {
            _factory = factory;
            _client = factory.CreateClient();
        }

        // [Fact]
        // public async Task CompleteAuthFlow_Register_EmailVerify_Login_ShouldSucceed()
        // {
        //     var registerContent = new { fullName = "Test User", email = "test@ex.com", password = "Pass123!" };
        //     var registerResponse = await _client.PostAsJsonAsync("/api/auth/register", registerContent);
        //     Assert.True(registerResponse.IsSuccessStatusCode);
        //
        //     var loginContent = new { email = "test@ex.com", password = "Pass123!" };
        //     var loginResponse = await _client.PostAsJsonAsync("/api/auth/login", loginContent);
        //     Assert.True(loginResponse.IsSuccessStatusCode);
        // }

        // [Fact]
        // public async Task TwoFactorFlow_Setup_Verify_Login_ShouldSucceed()
        // {
        //     // Setup 2FA
        //     // Verify 2FA
        //     // Login with 2FA code
        // }

        // [Fact]
        // public async Task PasswordResetFlow_Forgot_Reset_Login_ShouldSucceed()
        // {
        //     // Request password reset
        //     // Reset password with token
        //     // Login with new password
        // }

        // [Fact]
        // public async Task RefreshTokenFlow_ShouldGetNewAccessToken()
        // {
        //     // Login to get tokens
        //     // Use refresh token to get new access token
        //     // Verify new token works
        // }
    }
    */
}
